TODO:
    Initially Create a Signup Page.(implement from handling with dummy login.)
    After the signup page create a login page.
    Then create an Auth COntext PRovider (to store the use details with accessToken).
    Implement the Auth Context Provider on the main.jsx
    Create a ROuter to rote the Login and Signup page.
    Create a Protectes Dashboard and display user details.

Required Packages:
    npm install react-router-dom
    npm install react-hook form
    npm install axios
    npm install antd
    
    npm install @hookform/resolvers yup